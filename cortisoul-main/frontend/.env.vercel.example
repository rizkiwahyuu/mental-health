# Replace this placeholder with the public domain generated by the backend
# service in Railway. Do not use this placeholder as-is.
NEXT_PUBLIC_API_URL=https://YOUR-ACTUAL-BACKEND-DOMAIN.up.railway.app

# Optional. Leave empty until VAPID keys are configured on the backend.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
